apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskqueue-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taskqueue-worker
  template:
    metadata:
      labels:
        app: taskqueue-worker
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      # Must exceed LEASE_SECONDS + the longest expected handler runtime
      # so k8s doesn't SIGKILL the pod mid-job (otherwise the reaper has
      # to clean up the orphaned lease ~1 minute later).
      terminationGracePeriodSeconds: 90
      containers:
        - name: worker
          image: taskqueue:latest
          imagePullPolicy: Never  # use the image built into minikube's daemon
          ports:
            - name: metrics
              containerPort: 9090
          env:
            - name: ROLE
              value: worker
            - name: METRICS_PORT
              value: "9090"
            - name: WORKER_CONCURRENCY
              value: "2"
            - name: POLL_INTERVAL_S
              value: "5"
            - name: LEASE_SECONDS
              value: "60"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: DATABASE_URL
